name: "Scorecard DepsCheck Workflow"
on:
  push:
    branches: [main]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: |
          go build ./main.go
          ./main

      
      
